apiVersion: v1
kind: Service
metadata:
  name: music-recommendation-service
  namespace: music-app
spec:
  selector:
    app: music-recommendation
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000